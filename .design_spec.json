{
  "mandate_presets": [
    {
      "key": "pure_growth",
      "label": "Pure Growth",
      "description": "Maximize long-horizon capital appreciation; tolerates equity-or-higher volatility and deep multi-year drawdowns in exchange for upside capture. Income is irrelevant. Advisor note: young accumulator, 5y+ horizon, no income need. Typical horizon presets: 1Y / 3Y / 5Y.",
      "target_vol_pct": 22,
      "max_drawdown_tolerance_pct": 40,
      "growth_orientation": 1,
      "income_orientation": 0,
      "typical_horizon": "3-5 years (1Y/3Y/5Y presets)",
      "signal_weight_adjustments": {
        "trend": 0.35,
        "momentum": 0.25,
        "forecast": 0.3,
        "sentiment": 0.1
      },
      "rationale": "RECONCILIATION: target_vol 22% (B/C) not 28% (A) - 22% is the realized vol of a diversified all-equity sleeve (SPY sample vol 16%, high-beta single names 50%+); A's 28% conflates a concentrated single-name book with a diversified equity sleeve. max_dd 40% is the midpoint of A/B's 45% and C's 35%: a diversified equity portfolio's tolerable drawdown sits below the raw S&P 2008 -55% index extreme but above a 60/40, and a risk manager never signs uncapped loss. WEIGHTS (effective, sum=1.0): lift trend 0.30->0.35 and momentum 0.20->0.25 (Jegadeesh-Titman 3-12mo cross-section + Moskowitz time-series momentum are the best-documented long-horizon equity premia), keep forecast 0.30, cut sentiment 0.20->0.10 (news-lexicon sentiment is noise at multi-year horizons). Direction follows A/B (lift trend+momentum, cut sentiment), NOT C (which cut trend); riding momentum is correct but slow-trend confirmation is still the durable signal, so trend is lifted not cut. growth_orientation 1.0 / income 0.0 turns OFF every income insight rule entirely."
    },
    {
      "key": "balanced",
      "label": "Balanced",
      "description": "The neutral anchor: roughly equal growth and income, classic 60/40 risk budget. The ONLY preset that uses the engine's shipped base weights unchanged. Advisor note: mid-career, 3-15y horizon, wants to participate but sleep at night. Typical horizon presets: 6M / 1Y / 3Y.",
      "target_vol_pct": 12,
      "max_drawdown_tolerance_pct": 22,
      "growth_orientation": 0.6,
      "income_orientation": 0.4,
      "typical_horizon": "1-3 years (6M/1Y/3Y presets)",
      "signal_weight_adjustments": {
        "trend": 0.3,
        "momentum": 0.2,
        "forecast": 0.3,
        "sentiment": 0.2
      },
      "rationale": "UNANIMOUS across A/B/C: keep signals.WEIGHTS unchanged (trend 0.30/mom 0.20/fc 0.30/sent 0.20) because the composite score was originally tuned to this blend - it is the natural calibration anchor against which every other preset's tilt is defined. target_vol 12% is unanimous: realized vol of a 60/40 (0.6*16% equity + 0.4*5% bond approx 11.6%). max_dd RECONCILIATION: A=25, B=25, C=18 -> 22, the worst 60/40 drawdown of the last 40y with rebalancing (2008 approx -30% gross / approx -22% rebalanced; 2022 approx -22%); 22% is below A/B's pre-rebalancing 25% and above C's slightly tight 18%. 0.6/0.4 orientation arms BOTH growth and income insight rules at partial strength."
    },
    {
      "key": "income",
      "label": "Income Seeking",
      "description": "Prioritize durable distributions/yield with moderate price risk; capital appreciation is a bonus, not the mandate. Tolerates more drawdown than preservation in exchange for a higher distribution rate. Advisor note: retiree drawing approx 4%, dividends and coupons matter more than total return. Typical horizon presets: 6M / 1Y / 3Y.",
      "target_vol_pct": 10,
      "max_drawdown_tolerance_pct": 18,
      "growth_orientation": 0.25,
      "income_orientation": 0.85,
      "typical_horizon": "1-3 years (6M/1Y/3Y presets)",
      "signal_weight_adjustments": {
        "trend": 0.3,
        "momentum": 0.1,
        "forecast": 0.25,
        "sentiment": 0.35
      },
      "rationale": "RECONCILIATION target_vol: A=10, B=9, C=10 -> 10 (dividend-aristocrat / preferred-stock realized risk). max_dd: A=18, B=18, C=12 -> 18 (A/B agree; C's 12 is too tight for a book that still holds equity-like dividend names). growth_orientation 0.25 (A/B agree); income_orientation 0.85 (B's 0.85, between A's 1.0 and C's 0.8 - keeps a light non-income check active rather than disabling growth rules entirely). WEIGHTS: momentum 0.20->0.10 (chasing price momentum is antithetical to a hold-for-yield book and raises turnover/tax drag); sentiment 0.20->0.35 (for income names a dividend cut / downgrade / default headline IS distribution-cut risk and shows up in news before price - the engine's sentiment lexicon already flags cut/cuts/default/downgrade); forecast trimmed 0.30->0.25 (price drift matters less than distribution stability); trend kept 0.30. Chose B/A's sentiment-lift direction over C's near-balanced weights because headline risk is the single most relevant income signal."
    },
    {
      "key": "capital_preservation",
      "label": "Capital Preservation",
      "description": "Protect principal first; modest growth is a bonus. Suitable for money the client cannot afford to lose (near-term goal, or very risk-averse client). Advisor note: do not lose money. Typical horizon presets: 6M / 1Y.",
      "target_vol_pct": 6,
      "max_drawdown_tolerance_pct": 8,
      "growth_orientation": 0.1,
      "income_orientation": 0.5,
      "typical_horizon": "6 months - 2 years (6M/1Y presets)",
      "signal_weight_adjustments": {
        "trend": 0.4,
        "momentum": 0.1,
        "forecast": 0.2,
        "sentiment": 0.3
      },
      "rationale": "target_vol 6% is UNANIMOUS (A/B/C all 6%) - a short-duration bond + cash blend. max_dd RECONCILIATION: A=8, B=10, C=5 -> 8 (A's 8% is the defensible 'hard psychological line' for preservation clients; C's 5% is too tight to distinguish from the CD mandate, B's 10% too loose). growth 0.10 unanimous; income 0.50 (midpoint of A 0.50 / B 0.60 / C 0.40). WEIGHTS RECONCILIATION - the sharpest cross-design conflict: A/C lean SENTIMENT (0.50) as the risk-off early warning; B leans TREND (0.45) to de-risk decisively when SMA50<SMA200. Merged: trend 0.40 (B's 'don't get caught in a downtrend' logic dominates because a confirmed downtrend is a more reliable de-risk trigger than noisy headline sentiment) + sentiment 0.30 (keep a strong early-warning ear, A/C's instinct) + momentum 0.10 (minimize procyclical risk-taking) + forecast 0.20 (distrust optimistic point estimates when the job is to NOT lose). A >8% drawdown breaches the mandate and fires a HIGH insight."
    },
    {
      "key": "cd_alternative",
      "label": "CD / Cash Alternative",
      "description": "An explicit benchmark mandate: beat a CD/T-bill yield with near-cash stability. The implicit benchmark is the risk-free rate (rf, default 0.02), NOT equities. Any drawdown beyond a few percent defeats the purpose. Advisor note: client has cash earning nothing and asked if we can do a little better without real risk. Typical horizon presets: 6M / 1Y.",
      "target_vol_pct": 4,
      "max_drawdown_tolerance_pct": 4,
      "growth_orientation": 0.05,
      "income_orientation": 0.8,
      "typical_horizon": "3-18 months (6M/1Y presets)",
      "signal_weight_adjustments": {
        "trend": 0.35,
        "momentum": 0.05,
        "forecast": 0.25,
        "sentiment": 0.35
      },
      "rationale": "Tightest guardrails in the set. target_vol RECONCILIATION: A=3.5, B=4, C=4 -> 4.0 (B/C agree; brackets an ultra-short bond fund). max_dd: A=4, B=5, C=3 -> 4.0 (A's 4%; must be below preservation's 8% because a CD-alt that draws down more has mis-sold its purpose). growth 0.05 unanimous; income 0.80 (A's 0.80, between B 0.90 / C 0.60). WEIGHTS: momentum 0.05 (near-off - a cash-alt model must never trade on RSI swings; all three designs agree momentum is minimal here); forecast kept 0.25 so the long-horizon expected-return-vs-rf comparison drives the recommendation (A's point); trend 0.35 (B/C's de-risk lean) and sentiment 0.35 split the remaining weight to balance trend-following de-risk with headline early-warning. BENCHMARK: rf is read from a single configurable cd_benchmark_yield (default 0.02, the rate already in indicators.sharpe/sortino and backtest); EVERY return statement for this mandate quotes return MINUS the CD benchmark (excess), never gross. ANY projected p05 drawdown band breaching -4% fires a HIGH insight."
    }
  ],
  "portfolio_construction": {
    "method": "Reuse engine.portfolio.parse_model_file (parses Excel/.xlsx/.xlsm and CSV/.tsv/.txt, finds ticker+weight columns via alias matching, merges duplicate tickers, takes max(0,w), normalizes to sum 1.0, equal-weights when no weight column, caps at MAX_HOLDINGS). For analytics, build the portfolio return series via a union/rescaled synthetic-NAV reduction: (1) per holding, resolve a close series with data.resolve_series(ticker) (returns PriceSeries{symbol, close, source in 'sample'|'live'|'simulated'}); (2) compute per-holding daily simple returns r_i = close.pct_change(); (3) outer-join holdings on the UNION of dates; (4) each day, rescale target weights over holdings that have data that day and compute the portfolio return from that available set; (5) rebuild a synthetic NAV close as base 100 compounded returns. Feed nav UNCHANGED into indicators.indicator_frame / metrics_summary, forecast.forecast, signals.evaluate, backtest.run. This is a forward-analysis basis for mixed-history models, not a performance track record. Risk decomposition uses pairwise covariance/correlation so mismatched histories do not collapse the analysis to the shortest holding.",
    "rebalancing_assumption": "Target weights are used as the intended model weights, but when holdings have different histories the analyzed daily return rescales those weights across holdings available on that date. This keeps forward research usable for newly added or shorter-history holdings. STATE VERBATIM in the UI/docs: 'Portfolio model NAV uses a union/rescaled analysis basis; it is not a performance track record and will differ from a daily-rebalanced common-window book or a real buy-and-hold/drifting-weight account.'",
    "missing_history_policy": "Analyze the union of available holding dates and warn when histories differ. Exclude only holdings with no usable price data, rescale remaining weights, and report exclusions. If analyzed history is short (<252 sessions) surface a short-history insight; below 60 analyzed sessions refuse the portfolio. Long-horizon projections remain statistical extrapolations and should be treated with lower confidence when available history is short.",
    "concentration_metric": "Herfindahl-Hirschman Index HHI = sum(w_i^2) on normalized weights, with effective number of holdings N_eff = 1/HHI. RECONCILIATION of single-name cap: A/B=0.35, C=0.25 -> 0.35 default with a MANDATE-AWARE tightening to 0.15 for capital_preservation and cd_alternative (C's insight that a low-vol mandate cannot absorb single-name blowup risk). HHI bands: HHI<=0.15 (N_eff>=6.7) WELL-DIVERSIFIED; 0.15<HHI<=0.25 (N_eff 4-6.7) MODERATE (info); HHI>0.25 (N_eff<4) HIGH-concentration; for preservation/cd_alternative the MODERATE/HIGH line tightens to 0.20 (N_eff<5). The 0.25 HHI line = the equivalent of 4 equal-weight names and mirrors the antitrust 'highly concentrated' convention; 0.35 single-name = the RIC-style '75-5-10' regulatory diversification heuristic (no position dominating a quarter+ of the book). Also flag top-3 weight >0.60. MRC table feeds the 'concentration' insight category so trims target the vol-driving name.",
    "data_sourcing_fallback": "Reuse data.resolve_series's three-tier order: per ticker (1) sidebar store / cache via data.get; (2) LIVE via yfinance 5y history if data.HAS_YF and reachable; (3) deterministic SIMULATED GBM via data._simulate_for_ticker, seeded by SHA-256 over 'helios:' + ticker so simulated fallback is stable across processes. Uploaded instruments, resolved holding cache, and portfolio models are process-local, shared within the running app, and disappear on restart. Source flag ('sample'|'live'|'simulated') is returned per holding and propagated to a portfolio-level data_provenance / data_honesty block: {n_holdings, n_live, n_sample, n_simulated, simulated_weight_pct}. HONESTY: if simulated_weight_pct>0 the UI shows a persistent amber banner 'X% of portfolio weight uses SIMULATED price history - forecasts are illustrative, not market-calibrated' and every forecast/metric is tagged honesty in {'real' (0% sim), 'mixed' (0<sim<100%), 'simulated' (100%)}. No simulated number is ever presented as real market data. PARAMETERS: synthetic NAV seed price 100.0; cd_benchmark_yield / rf configurable (env HELIOS_RF, default 0.02)."
  },
  "long_horizon_forecast": {
    "horizon_presets": "Short slider and short API horizons are 5-90 trading days. Invalid short horizons return JSON 400 errors instead of silently falling back. Long presets map to trading-day counts: 6M=126, 1Y=252, 3Y=756, 5Y=1260 (21 trading days/month, 252/year, round(years*252)). A horizon_class field tags each: H<=90='short' (per-step daily MC, full Ridge feature regression, existing fine cone); H>90='long' (drift from the shrinkage/regime model below, wider uncertainty, coarser cone explicitly labeled 'strategic projection, not a trading forecast'). The UI visually separates the two.",
    "drift_methodology": "CRITICAL (unanimous across A/B/C): the next-day Ridge drift from forecast.forecast (model_mu, already Sharpe-capped at 1.5) is a SHORT-horizon signal and must NOT be extrapolated 1260 days - that is the momentum-blowup overfitting trap. The short Ridge mu is used ONLY for H<=90. For H>90, annualized expected drift mu_LT is a SHRINKAGE blend toward a defensible long-run anchor: mu_LT = (1-lambda)*mu_hist_capped + lambda*mu_anchor. ANCHOR (merge of A's per-mandate prior and B/C's CAPM): mu_anchor = rf + ERP*growth_orientation, with rf=0.02 (reuses indicators.sharpe) and ERP=0.045 (deliberately BELOW the historical realized approx 5.5% so we under-promise; B/C agree on 0.045). This makes the anchor scale smoothly with each mandate's growth_orientation (pure_growth 1.0 -> 0.02+0.045=6.5%; balanced 0.6 -> 4.7%; income 0.25 -> 3.1%; preservation 0.10 -> 2.5%; cd_alternative 0.05 -> approx 2.2% approx rf) - this reproduces A's intended per-mandate anchors (growth ~7%, balanced ~5%, income ~4%, preservation ~2.5%, cd~rf) from ONE defensible CAPM formula rather than five hardcoded magic numbers. mu_hist_capped = portfolio trailing-252d annualized mean return, capped so implied annualized Sharpe <=1.5 (reuse the engine's existing |mu_ann/vol_ann|<=1.5 discipline). SHRINKAGE WEIGHT lambda = clip(H/1260, 0, 0.8) (A's schedule, reframed as the weight ON THE ANCHOR so it is monotone increasing and bounded): 6M lambda=0.10, 1Y=0.20, 3Y=0.60, 5Y=0.80 - the further out, the more drift comes from the defensible anchor and the less from the noisy trailing signal; at 5Y, 80% is anchor. (A's H/1260 and C's 126/(126+H) encode the SAME intent with opposite labels; A's is adopted because it is monotone in horizon and bounded at 0.8, never letting the noisy signal fully vanish nor fully dominate.) FINAL CAP: after blending, re-cap mu_LT so implied annualized Sharpe (mu_LT-rf)/sigma_ann <= 0.60 for long horizons (C's 0.60, chosen over B's 0.75 - no diversified long-only book sustains a >0.6 Sharpe over 3-5y; half the 1.5 tactical cap encodes 'we believe our edge less the further out we look').",
    "volatility_scaling": "Uncertainty cone widens as sigma_ann*sqrt(t_years), standard Brownian/GBM scaling (verified: sqrt(126/252)=0.707, sqrt(252/252)=1.0, sqrt(756/252)=1.732, sqrt(1260/252)=2.236). sigma_ann for long horizons = portfolio trailing-252d daily-return std * sqrt(252) (B/C's full-window vol, NOT the 60d short-horizon window - multi-year cones need a full-cycle vol estimate). REGIME WIDENING (merge of A's kappa schedule and B/C's data-driven bump): effective sigma_eff = sigma_252 * max(kappa_H, sigma_60/sigma_252), where kappa_H is the horizon-uncertainty floor {1.10 at 6M, 1.20 at 1Y, 1.35 at 3Y, 1.50 at 5Y, linear interp} (A's schedule, encoding that volatility is non-stationary and itself uncertain) and sigma_60/sigma_252 widens to the current regime if recent vol is elevated (C's 'currently stormy' flag). The cone is only ever WIDENED, never narrowed - the only safe direction for a risk manager to err. Additionally the OUTER bands (p05/p95) use sigma_eff*1.15 to acknowledge parameter uncertainty in sigma itself (B/C's 1.15x vol-of-vol haircut). Monte-Carlo uses CORRELATED holding shocks via Cholesky(Sigma_60) so genuine diversification correctly narrows the cone relative to summing standalone vols.",
    "outputs": "Per long horizon: (1) terminal portfolio value bands p05/p25/p50/p75/p95 (existing band structure, on a $10,000 base, NAV multiples + %); (2) prob_positive = P(terminal>start); (3) prob_meets_mandate = P(annualized return over horizon >= mandate target), targets derived from the SAME CAPM anchor for consistency: pure_growth 6.5%, balanced 4.7%, income 3.1%, capital_preservation 2.5%, cd_alternative = cd_benchmark_yield (approx rf); (4) annualized CAGR band: (p_q_terminal/start)^(252/H)-1 for q in {05,25,50,75,95}, e.g. '5Y median 5.8%/yr, p05-p95 -2.1% to 13.4%/yr'; (5) max-drawdown-WITHIN-path distribution (median and p95 of worst intra-path drawdown) AND prob_breach_maxdd = share of paths whose path drawdown exceeds the mandate's max_drawdown_tolerance_pct (C's point - binding for preservation 8% and cd_alternative 4%). EVERY long panel carries the disclaimer 'Statistical projection, not a guarantee; bands widen with the square root of time and assume the trailing-vol regime persists' plus an explicit 'low confidence beyond available history' flag when H>available_history_days.",
    "parameters": "rf / cd_benchmark = 0.02 (existing, configurable). ERP = 0.045 (below historical realized approx 5.5%). short-horizon drift Sharpe cap = 1.5 (existing, unchanged). long-horizon Sharpe cap = 0.60. shrinkage anchor weight lambda = clip(H/1260, 0, 0.8) {6M 0.10, 1Y 0.20, 3Y 0.60, 5Y 0.80}. kappa regime floor = {6M 1.10, 1Y 1.20, 3Y 1.35, 5Y 1.50}, taken as max with sigma_60/sigma_252. outer-band vol inflation = 1.15x. vol window: 60d short / 252d long. drift window: 252d historical. n_paths: 2000 for short horizons (existing forecast default, MC tail SE approx 1/sqrt(2000)=2.2%), RAISED to 4000 for LONG horizons (compromise between A's 2000 and C's 5000; SE approx 1.6%, and a 4000x1260 daily sim benchmarked at 48ms / 40MB - fully tractable). RNG seed = 12345 (matches forecast.py for reproducibility). horizon day-counts 126/252/756/1260. base value $10,000.",
    "step_resolution": "H<=90 -> DAILY steps (existing per-day feature dynamics preserved). H>90 -> MONTHLY steps, count = round(H/21) (6M=6, 1Y=12, 3Y=36, 5Y=60 steps), with monthly drift = mu_LT/12 and monthly vol = sigma_eff/sqrt(12). VERIFIED EQUIVALENT: a 4000x60 monthly 5Y sim (2ms) reproduces the daily-sim terminal p05/p50/p95 within MC noise (monthly 60.7/118.1/224.4 vs daily 61.8/117.8/230.3), so monthly stepping is a pure performance win with zero statistical cost under GBM and keeps the path matrix <=4000x60. Terminal-value percentiles taken at the final step; intra-path drawdown computed on the monthly path. Only approx 120 evenly-spaced percentile-band points are serialized to the client (reuse backtest._downsample_index), not all 1260 days.",
    "why_intentional": "Every constant traces to a defensible source, surfaced in a single CONFIG dict with an inline origin comment so NO number is magic: sqrt-time from Brownian motion (numerically verified); the 1.5 short / 0.60 long Sharpe caps are the engine's existing anti-extrapolation discipline halved-and-then-some for the long horizon (a sustained >0.6 Sharpe over 3-5y is historically rare for long-only books); lambda=clip(H/1260,0,0.8) formalizes Bayesian shrinkage / credibility - trust the long-run anchor more as horizon grows, never letting a noisy 1-day Ridge signal dominate a 5-year projection nor fully vanish; mu_anchor=rf+ERP*growth_orientation derives all five per-mandate priors from ONE CAPM formula (replacing five hardcoded constants), with rf reused from indicators and ERP intentionally below realized history; kappa + sigma_60/sigma_252 + 1.15x outer-band inflation all bias the cone toward MORE uncertainty, the only safe direction; n_paths 2000/4000 and seed 12345 inherit from the existing forecast for consistency."
  },
  "conviction_rationale": {
    "component_templates": "Each of the four components emits ONE numbers-backed clause built from the raw/weight/contribution values signals.evaluate already computes, assembled in DESCENDING order of |contribution| (dominant driver leads - C's ordering). TREND: 'Trend {dir} (raw {trend_raw:+.2f}, base wt {0.30:.0%} -> effective {w_trend:.0%}, contrib {contrib_trend:+.2f}): SMA50 {above/below} SMA200, price {above/below} SMA50, MACD histogram {positive/negative}' (uses the existing _trend_score 0.5 golden/death-cross + 0.25 price-vs-SMA50 + 0.25 MACD-sign structure). MOMENTUM: 'Momentum {oversold/overbought/neutral} (RSI {rsi:.0f} vs 30/70 bands, raw {mom_raw:+.2f}, contrib {contrib_mom:+.2f})'. FORECAST: 'Model projects {exp_ret:+.1f}% over {H}d (p(up)={prob_up:.0%}, ann. drift {ann_drift:+.1f}% capped at Sharpe {1.5 short / 0.60 long}; raw {fc_raw:+.2f}, contrib {contrib_fc:+.2f}); OOS directional accuracy {dir_acc:.0%} on {n_test} days' - surfaces forecast.quality so conviction is tied to MEASURED out-of-sample skill, not just the point estimate. SENTIMENT: 'News {label} (aggregate {agg:+.2f} across {count} headlines; raw {sent_raw:+.2f}, contrib {contrib_sent:+.2f})'. Then a VOL-PENALTY clause when it bites: 'Conviction scaled x{vol_penalty:.2f} because annualized vol {vol:.0%} exceeds the 35% penalty threshold (floor 0.60 at vol>=115%)'. Then a MANDATE-FIT clause: 'Mandate {mandate}: target vol {tgt_vol:.0%} vs realized {vol:.0%} ({within/breach}); weights tilted from base {base} to effective {effective}'. For PORTFOLIOS prefix: 'This MODEL ({N} holdings, top weight {top_w:.0%} in {top_ticker})...' so the advisor knows it describes the blend.",
    "intentional_parameters": "Every threshold is the SHIPPED codebase value, quoted in the rationale so none reads as magic, and GENERATED from a single CONFIG dict so text and math can never drift apart. ACTION bands BUY>+0.25 / SELL<-0.25 (signals._to_action) - quoted as 'score {score:+.2f} vs BUY line +0.25'; 0.25 means at least a quarter-of-full-scale weighted move, so a single component can't trigger a trade alone (trend at full 0.46 effective * 0.5 cross = 0.23 needs corroboration). conviction_pct = |score|*100 (signals.evaluate). Forecast saturation +/-5% over horizon (signals._forecast_score /5.0) - stated so a +12% forecast still caps the component at 1.0. RSI bands 30/70 (signals._momentum_score, Wilder's standard oversold/overbought lines), mid-range tilt x0.4. Vol-penalty clip(1-max(0,vol-0.35)*0.5, 0.6, 1.0) (signals.evaluate) - kicks in above 0.35 annualized vol, slope 0.5, floored at 0.60, so a 55% vol name keeps 0.90x and a 115%+ vol name hits the floor; EXPLAINED in words. Trend sub-weights 0.5 golden/death cross + 0.25 price-vs-SMA50 + 0.25 MACD-sign (signals._trend_score) - the cross gets double weight as the slower, more reliable regime signal. Base weights 0.30/0.20/0.30/0.20 (signals.WEIGHTS), MULTIPLIED/overridden by the mandate's signal_weight_adjustments (already normalized to sum 1.0 in the presets) - the rationale prints BOTH base and effective so the mandate's influence is auditable. MANDATE-FIT OVERLAY (C's addition): after weights apply, conviction is additionally scaled by mandate_fit in [0.5,1.0]: if the action would push projected vol above target_vol, fit = max(0.5, target_vol/projected_vol), and the rationale appends 'conviction reduced x{fit} - signal vol {vol:.0%} exceeds {mandate} target {tgt:.0%}'.",
    "overall_format": "A structured conviction object: {action, score, conviction_pct, conviction_band, components:[{name, raw, base_weight, effective_weight, contribution, clause}], vol_penalty, mandate_fit, headline_rationale, caveats:[]}. headline_rationale reuses the existing signals._rationale verb map (BUY=Constructive / SELL=Cautious / HOLD=Balanced) + '({conviction_pct:.0f}% conviction, {band})' + the top-2 component clauses (by |contribution|) + the mandate-fit clause + any caveat. conviction_band buckets |score|*100: <20 'low', 20-45 'moderate', 45-70 'high', >70 'very high' - stated so a 30% conviction is never oversold. caveats AUTO-APPEND when: OOS directional accuracy <0.50 ('forecast shown but model has no measured edge - directional accuracy {dir_acc:.0%} <= coin-flip; treat the forecast contribution skeptically'); analyzed history <252d; or data_honesty.simulated_weight_pct>0. The whole rationale is a deterministic string assembled from the numbers - no LLM, fully offline, reproducible, unit-testable - so identical inputs always yield the identical explanation."
  },
  "insight_rules": [
    {
      "id": "conc_single_name",
      "category": "concentration",
      "trigger": "any normalized weight w_i > 0.35 (tightened to > 0.15 for capital_preservation and cd_alternative mandates)",
      "severity": "high",
      "message": "{sym} is {w:.0%} of the portfolio - above the {cap:.0%} single-name limit for a {mandate_label} mandate. Its marginal risk contribution is {mrc:.0%} of total portfolio volatility.",
      "suggested_action": "Trim {sym} toward {cap:.0%} and redistribute into uncorrelated holdings, or document it as a deliberate concentrated single-name thesis that breaches the mandate's diversification norm.",
      "rationale": "A >35% single weight means one issuer's idiosyncratic shock (earnings, fraud, downgrade) can breach the mandate's drawdown tolerance alone. 35% is the RIC-style regulatory diversification line (A/B); tightened to 15% for low-vol mandates because a preservation/CD book cannot absorb single-name blowup risk (C). Quotes MRC, not just weight, so the advisor trims the vol-driving name."
    },
    {
      "id": "conc_hhi",
      "category": "concentration",
      "trigger": "HHI = sum(w_i^2) > 0.25 (N_eff = 1/HHI < 4), tightened to > 0.20 (N_eff < 5) for capital_preservation/cd_alternative, AND no single weight already flagged by conc_single_name",
      "severity": "medium",
      "message": "Portfolio behaves like only {N_eff:.1f} independent holdings (HHI {HHI:.2f}); diversification is thinner than the holding count suggests.",
      "suggested_action": "Add 2-4 lowly-correlated positions (correlation <0.4) to lift N_eff above 4-5, where diversification benefit flattens; concentration this high makes drawdowns lumpy and the forecast cone understate tail risk.",
      "rationale": "HHI 0.25 = four equal-weight names, the practical floor below which idiosyncratic single-name risk dominates; mirrors the antitrust 'highly concentrated' convention and the concentration_metric thresholds. Severity medium (not high) since no single name breaches the cap."
    },
    {
      "id": "vol_above_mandate",
      "category": "mandate-fit",
      "trigger": "realized trailing-252d annualized vol > mandate.target_vol_pct/100 * 1.15 (15% tolerance band)",
      "severity": "high",
      "message": "Realized volatility {vol:.0%} exceeds the {mandate_label} target of {tgt:.0%} by more than 15% - this portfolio is running hotter than its stated mandate.",
      "suggested_action": "Shift weight from the highest-vol sleeves toward lower-vol/income holdings (or add a cash/short-duration sleeve) until realized vol is within {tgt:.0%}, or re-rate the mandate to a tier whose risk budget matches. The mandate-fit module quantifies how much each holding must shrink.",
      "rationale": "RECONCILIATION: A=1.15x, B/C=1.20x/1.15x tolerance -> 1.15x (the tighter, more protective band). The mandate's target_vol is a contract with the client; a 15% band avoids false alarms on noise while catching genuine drift. This is the single most common and most actionable finding on uploaded models."
    },
    {
      "id": "drawdown_breaches_tolerance",
      "category": "risk",
      "trigger": "historical max_drawdown (indicators.max_drawdown on the NAV) worse than -mandate.max_drawdown_tolerance_pct, OR long-horizon MC prob_breach_maxdd over the 1Y horizon > 0.20",
      "severity": "high",
      "message": "Historical max drawdown {maxdd:.0%} breaches the {mandate_label} tolerance of {tol:.0%}, and {p_breach:.0%} of 1-year simulated paths breach it again. There is a {p_loss:.0%} chance of ending the {horizon} horizon below today's value.",
      "suggested_action": "De-risk (reduce equity/high-beta, add a defensive sleeve) to bring projected drawdown within {tol:.0%}, or with the client formally re-rate the mandate. For CD/preservation mandates this is a HARD breach - the portfolio is not fit for purpose.",
      "rationale": "Ties realized max-DD AND the long-horizon MC drawdown distribution to each mandate's max_drawdown_tolerance (merge of B's realized check and A/C's MC-path check). A >20% one-year breach probability is actionable, not hypothetical (C). Especially binding for cd_alternative (4%) and capital_preservation (8%) where principal protection is the whole point."
    },
    {
      "id": "high_pairwise_correlation",
      "category": "diversification",
      "trigger": "mean off-diagonal correlation of the holding return matrix C > 0.70 (computed from pairwise available returns)",
      "severity": "medium",
      "message": "Holdings move together: average pairwise correlation is {corr:.2f}. Diversification benefit is limited - the portfolio behaves like {N_eff_corr:.1f} independent bets.",
      "suggested_action": "Add holdings from different sectors/asset classes (bonds, commodities, international) with correlation <0.4 to genuinely reduce vol.",
      "rationale": "High correlation means nominal diversification (many names) overstates real diversification; 0.70 is where correlation-adjusted effective breadth collapses. Uses correlation matrix C from portfolio_construction. From Design A; B/C agree correlation-aware breadth matters."
    },
    {
      "id": "no_forecast_edge",
      "category": "signal",
      "trigger": "forecast.quality.directional_accuracy is not None AND directional_accuracy <= 0.50 (escalate to a stronger warning when |forecast raw| >= 0.8 AND dir_acc < 0.52: a confident call on an unskilled model)",
      "severity": "medium",
      "message": "The return model shows no measured edge: out-of-sample directional accuracy is {dir_acc:.0%} on {n_test} days (coin-flip is 50%){', yet the forecast is leaning strongly' if strong_call}.",
      "suggested_action": "Down-weight or ignore the forecast component for this portfolio; rely on trend, mandate-fit, and realized risk signals instead until OOS accuracy clears 55%. Disclose the weak forecast to the client rather than quoting the point estimate.",
      "rationale": "Honesty requirement: the engine already computes walk-forward OOS metrics (forecast._evaluate, 70/30 split). A confident forecast on an unskilled model is worse than no forecast; <=0.50 is indistinguishable from a coin flip (A/B), and the 0.52+strong-call escalation is C's sharper test. The insight panel must say so rather than letting a meaningless forecast inflate conviction."
    },
    {
      "id": "vol_penalty_active",
      "category": "signal",
      "trigger": "signals.evaluate vol_penalty < 0.85 (annualized vol > ~0.65); informational",
      "severity": "info",
      "message": "Conviction was reduced {pct_cut:.0f}% by the volatility penalty because realized vol is {vol:.0%} (penalty floor 0.60 at vol>=115%). The raw signal is stronger than the displayed conviction.",
      "suggested_action": "If the high vol is expected for this asset class the penalty is doing its job; if not, investigate what is driving the volatility and size positions smaller, requiring trend+forecast agreement before acting.",
      "rationale": "Explains WHY conviction looks muted so an advisor isn't confused by a strong-looking setup showing modest conviction - turns the opaque vol_penalty haircut into an honest talking point (B/C). Info severity: the tool still works, this is transparency."
    },
    {
      "id": "income_yield_gap",
      "category": "income",
      "trigger": "mandate.income_orientation >= 0.40 and the mandate has an income floor, but holding-level yield data is not available",
      "severity": "medium",
      "message": "This is an income-oriented mandate ({mandate_label}) but holding-level yield data is not available, so income adequacy versus the {floor:.1%} floor cannot be confirmed.",
      "suggested_action": "Supply estimated yields or add known income holdings (dividend equity, REITs, preferreds, bond funds) so the model can verify it pays the income it promises.",
      "rationale": "Gated on income_orientation>=0.40 so it never fires for growth mandates. Until holding-level yield inputs exist, the honest behavior is to disclose the data gap rather than estimate yield from price history."
    },
    {
      "id": "growth_overdefensive",
      "category": "mandate-fit",
      "trigger": "mandate.growth_orientation >= 0.8 AND realized trailing-252d annualized vol < mandate.target_vol_pct/100 * 0.5",
      "severity": "low",
      "message": "Growth mandate but realized vol {vol:.0%} is less than half the {tgt:.0%} target - the portfolio may be under-risked for its objective, leaving expected return on the table.",
      "suggested_action": "Consider adding higher-beta/growth holdings or reducing cash/bond ballast to align risk-taking with the growth objective.",
      "rationale": "Mandate-fit cuts both ways: an over-risked preservation book is bad, but a massively under-risked growth book is also a mismatch. The 0.5x target-vol floor flags clear under-utilization without nagging on minor shortfalls. From Design A; the symmetric complement to vol_above_mandate."
    },
    {
      "id": "mandate_label_mismatch",
      "category": "mandate-fit",
      "trigger": "mandate in {capital_preservation, cd_alternative} AND (growth-style sleeve weight > 0.30 OR realized annualized vol > 1.5x mandate.target_vol_pct)",
      "severity": "high",
      "message": "A {mandate_label} book is carrying {growth_w:.0%} growth-style risk and {vol_mult:.1f}x its target volatility - this is a growth book wearing a preservation/CD label.",
      "suggested_action": "Rotate the growth sleeve into low-vol/income holdings, or correct the mandate with the client. Selling preservation while running growth risk is the costliest mis-classification.",
      "rationale": "Mandate-label mismatch is the failure most likely to harm a client and trigger a complaint (C's FIT-001). Checked first and rated high regardless of recent performance, because a tail event - not the average - is what breaches a preservation client's trust."
    },
    {
      "id": "simulated_data_warning",
      "category": "risk",
      "trigger": "data_provenance.simulated_weight_pct > 0",
      "severity": "high",
      "message": "{pct:.0%} of portfolio weight uses SIMULATED price history ({n_sim} of {n_total} holdings had no live/sample data). Forecasts and metrics for this portfolio are illustrative, not market-calibrated.",
      "suggested_action": "Provide real price history (CSV upload or enable live data) for: {sim_syms}. Until then, treat every return/risk number as a structural illustration only.",
      "rationale": "RECONCILIATION of severity: A/B treat simulated data as HIGH (it invalidates downstream calibration), C as info (the tool still works). Resolved to HIGH because presenting simulated bands as real in a client meeting is the exact honesty failure all three designs forbid - it must be loud and persistent, with the simulated weight quantified."
    },
    {
      "id": "short_history_low_confidence",
      "category": "risk",
      "trigger": "analyzed portfolio history < 252 trading days; escalating: <252 = info, <126 = medium",
      "severity": "info",
      "message": "Portfolio analyzed history is only {n_days} trading days (~{months:.0f} months). Mixed-history holdings are weight-rescaled over their available dates, so long-horizon projections extrapolate beyond some observed data.",
      "suggested_action": "Supply longer price history for the shortest-history holdings or substitute longer-history proxies before relying on 1Y/3Y/5Y projections. Short-horizon (<=90d) signals remain usable.",
      "rationale": "Annualized vol/return and the long-horizon cone need enough data to be stable; 252 days = 1 year is the minimum for more credible annualization. The union/rescaled methodology avoids discarding longer-history holdings but must be disclosed as forward-analysis, not a performance track record."
    }
  ],
  "evidence_lab_walk_forward": {
    "source": "engine/evidence_lab.py analyze_series/_walk_windows",
    "parameter_clamps": {
      "horizon_days": "clip to 5-252, default 21",
      "train_window": "clip to 90-756, default 252",
      "step": "clip to 5-63, default 21",
      "decay_horizons": "sorted union of {5, horizon_days, 63}",
      "rationale": "Clamped API inputs keep walk-forward runs tractable and comparable: 21d matches the default signal horizon, 252d training is one year of context, and the fixed 5/63 decay endpoints let every target answer 'does the edge decay with horizon' on the same axes."
    },
    "history_gate": {
      "trigger": "blocked unless len(clean close) >= train_window + max(decay_horizons) + 5 sessions",
      "rationale": "Anything shorter cannot produce even a handful of non-overlapping windows, so a hit rate would be noise presented as evidence."
    },
    "provenance_gate": {
      "trigger": "blocked unless data_provenance.eligible_for_real_research (sample/simulated data always blocked; models via build_series(allow_sample=False, allow_simulated=False))",
      "rationale": "Walk-forward 'proof' on demo or simulated prices is the exact honesty failure the platform forbids; the lab refuses rather than caveats."
    },
    "action_bands": {
      "buy": "historical signal score > +0.15",
      "sell": "historical signal score < -0.15",
      "hold": "otherwise",
      "rationale": "Wider than the live +/-0.25 trade bands so the lab measures more directional calls and false positives; it grades the signal, it never places the trade."
    },
    "paper_hit_rule": {
      "definition": "evidence = alpha vs benchmark when available else raw forward return; BUY/ADD/OVERWEIGHT hit iff evidence > 0, SELL/REDUCE/UNDERWEIGHT hit iff evidence < 0, HOLD/REVIEW hit iff evidence >= -1.0",
      "rationale": "Directional calls must land on the right side of zero, while HOLD tolerates a 1% drag because 'do nothing' is only wrong when it materially lags the benchmark (shared with the Signal Journal so both layers score identically)."
    },
    "false_positive_definition": {
      "trigger": "BUY/SELL windows whose paper hit is False, reported as count and rate over directional windows only",
      "rationale": "A confident directional call that fails is the costliest error for an advisor, so it is tracked separately from the blended hit rate."
    },
    "confidence_bands": {
      "definition": "p05/p25/p50/p75/p95 percentiles plus a normal-approximation 90% CI (mean +/- 1.645*SE) on forward return, alpha, and hit rate",
      "rationale": "Bands stop a lucky mean from being read as skill; 1.645 is the standard two-sided 90% z so the interval is deliberately conservative for small window counts."
    },
    "no_lookahead": {
      "definition": "signal scores and regime labels are computed once on the full series from strictly backward-looking inputs and sliced at each signal date (equivalence to frozen-prefix recomputation asserted in tests)",
      "rationale": "Slicing precomputed causal series is a pure performance win only because every input is backward-looking; the test assertion keeps that invariant honest."
    },
    "prospective_layer": {
      "definition": "same-target Signal Journal entries reported alongside historical windows with status not_started/measuring/active/measured",
      "rationale": "Historical walk-forward and live paper tracking answer different questions; showing both prevents backtest evidence from masquerading as prospective performance."
    }
  },
  "governance_risk_gate": {
    "source": "engine/model_governance.py _risk_gate/_risk_limits/_risk_limit_violations/record_event",
    "gate_behavior": {
      "definition": "can_approve is false and state is 'blocked' while any risk-limit violation is active; approval events are rejected with HTTP 400 until resolved",
      "rationale": "A committee should never be able to sign off a model that already breaches its own written limits."
    },
    "max_single_position_pct": {
      "limit": "library template risk_limits when the model came from a template, else mandate single_name_cap * 100 (default 35%)",
      "rationale": "Reuses the mandate/insight single-name cap so governance and insights can never disagree about the same breach."
    },
    "min_holdings": {
      "limit": "template value or 5 for uploaded models",
      "rationale": "Below five holdings a 'model' is a concentrated bet; the floor forces that to be an explicit template decision, not a default."
    },
    "total_weight_pct": {
      "limit": "sum of weights must be within 100% +/- 0.5pp",
      "rationale": "Half a point absorbs rounding from percentage inputs while catching genuinely unnormalized allocations."
    },
    "committee_note": {
      "limit": "approve/reject requires a note of at least 5 characters",
      "rationale": "An unexplained approval is worthless in an audit; five characters is the minimal 'you must say something' bar."
    },
    "pin_verification": {
      "definition": "when HELIOS_GOVERNANCE_APPROVER_PIN or _PIN_HASH is set, approve/reject requires signer_name, signer_role, committee, and a PIN matched as SHA-256 via hmac.compare_digest; otherwise decisions record as unverified local_attestation",
      "rationale": "Hash-form config keeps the plain PIN out of shared-machine environments, and constant-time comparison closes the timing side channel; without a PIN the packet still records who claimed the decision but marks it unverified."
    }
  },
  "model_validation_thresholds": {
    "source": "engine/model_validation.py _validation_score/_grade/_drift_alerts",
    "score_weights": {
      "hit_rate_pct": 0.35,
      "alpha_component": 0.25,
      "false_positive_component": 0.2,
      "decay_component": 0.15,
      "governance_component": 0.05,
      "rationale": "Hit rate and alpha carry 60% because they are the direct paper-performance evidence; false positives and decay penalize fragile edges; governance gets a deliberate 5% so process state can tilt a ranking but never rescue a model with no measured edge."
    },
    "alpha_component": {
      "definition": "clamp(50 + avg_alpha_pct * 10, 0, 100)",
      "rationale": "Centers 'no alpha' at 50 and saturates at +/-5% average alpha, beyond which more claimed edge is more likely noise than skill."
    },
    "false_positive_component": {
      "definition": "clamp(100 - false_positive_rate_pct, 0, 100)",
      "rationale": "A direct inversion keeps the penalty linear and legible: every extra point of false-positive rate costs exactly one component point."
    },
    "governance_component": {
      "definition": "75 base, -25 when risk_limit_state is breach, +10 when approval_status is approved, clamped 0-100",
      "rationale": "Breaching written limits must cost more than committee approval earns, so paperwork can never outweigh an active violation."
    },
    "grade_bands": {
      "A": ">= 80",
      "B": ">= 65",
      "C": ">= 50",
      "D": "< 50",
      "rationale": "Coarse letter bands force conversation about evidence rather than false precision from a two-decimal score."
    },
    "champion_selection": {
      "definition": "eligible models sorted by (validation_score, window_count, name) descending; top row is champion, remaining eligible rows are challengers, ineligible rows stay blocked",
      "rationale": "Window count breaks score ties toward the model with more measured evidence; blocked models can never rank because validation must not bypass provenance gates."
    },
    "drift_alerts": {
      "governance_breach": "high when risk_limit_state is breach",
      "negative_walk_forward_alpha": "high when avg_alpha_pct < 0",
      "false_positives_elevated": "medium when false-positive rate >= 35%",
      "signal_decay_warning": "medium when longest-horizon decay alpha < 0",
      "prospective_drift": "medium when Signal Journal avg alpha < 0",
      "rationale": "Zero is the honest line for alpha alerts (paper evidence is net-negative), and 35% false positives means roughly one in three directional calls failed - past the point an advisor should quote the signal without caveats."
    }
  },
  "risk_pack_limits": {
    "source": "engine/risk_exposure.py _concentration_warnings/_volatility_budget/_correlation_clusters/_liquidity_flags/_risk_posture",
    "single_name_warning": {
      "trigger": "top holding >= 20% weight (medium), >= 30% (high)",
      "rationale": "Warns a full tier earlier than the 35% governance block so the pack prompts a trim conversation before the hard gate fires."
    },
    "top_5_concentration": {
      "trigger": ">= 60% combined weight in the top five holdings (medium)",
      "rationale": "Mirrors the insight-layer top-3 > 60% intent at pack granularity: past 60% the tail holdings are decoration, not diversification."
    },
    "sector_concentration": {
      "trigger": "any sector >= 45% of model weight (medium)",
      "rationale": "45% is roughly double a diversified benchmark's largest sector weight, the point where one macro theme drives the book."
    },
    "volatility_budget": {
      "trigger": "over_budget (high) when realized annualized vol > mandate target_vol_pct with no tolerance band",
      "rationale": "The pack states the raw gap for client review; the 1.15x insight-layer band still governs when the mandate-fit insight fires."
    },
    "correlation_clusters": {
      "high_correlation": "pairwise correlation >= 0.75",
      "diversifier": "pairwise correlation <= 0.20",
      "rationale": "0.75 is where two holdings behave as one position under stress; 0.20 marks pairs that still plausibly diversify, with the standard caveat that correlations rise in crises."
    },
    "liquidity_flags": {
      "score_tiers": "ADV >= $5B -> 95, >= $1B -> 85, >= $250M -> 70, below -> 55, unmapped ticker -> 40",
      "flags": "score < 45 unknown_liquidity, < 65 watch, else normal",
      "basis": "observed 60-day live/uploaded dollar volume when available, else a static public large-cap proxy",
      "rationale": "Observed volume outranks the static proxy so real uploaded evidence always wins; unmapped names default below the watch line because unknown liquidity must be verified, never assumed."
    },
    "scenario_shocks": {
      "definition": "fixed deterministic sector/exposure shocks (equity -10%, rates +100bps, growth compression, defensive rotation, liquidity stress) plus a growth-factor unwind when growth exposure > 40%",
      "rationale": "Hardcoded shock tables are transparent and reproducible - stress illustrations, not forecasts, and never Monte-Carlo dressed up as scenarios."
    },
    "risk_posture": {
      "definition": "elevated when vol budget is over_budget or any high-severity stress/concentration row; watch when any liquidity flag is non-normal; else review_ready",
      "rationale": "One conservative word summarizes the pack, and the ladder only escalates - a clean posture requires every check to pass."
    }
  },
  "data_quality_alert_severity": {
    "source": "engine/data_quality.py dashboard_payload/alert_inputs + persistence.sync_data_quality_alerts",
    "blocker_rules": {
      "short_history_below_research_floor": "non-sample symbol rows < min_research_rows (default 60)",
      "missing_model_data": "a model holding has no eligible real history",
      "zero_coverage_model": "coverage gap where 0 holdings have real research coverage",
      "rationale": "Blockers are exactly the states where research output would be wrong, not merely degraded - below 60 rows or with missing holdings there is no honest analysis to show."
    },
    "warning_rules": {
      "stale_symbols": "latest data older than stale_days (default 7 calendar days)",
      "short_histories": "rows below institutional target (default 252) but at or above the research floor",
      "refresh_failures": "provider refresh attempts logged with status error",
      "refresh_observability_gaps": "live-sourced symbols with no persisted refresh attempt in the local log",
      "partial_coverage_and_source_conflicts": "models with mixed real/sample/missing holdings or mixed sources",
      "rationale": "Warnings mark evidence that is usable but not presentable to a client unchallenged; a week of staleness spans any market holiday cluster without flagging every weekend, and 252 rows is the one-year annualization floor."
    },
    "research_ready": {
      "definition": "workspace is research_ready only when blocker count is 0 and at least one symbol or model is individually research-ready",
      "rationale": "An empty workspace with no blockers is not 'ready' - readiness requires actual eligible evidence, not just the absence of problems."
    },
    "alert_lifecycle": {
      "definition": "alerts persist with stable ids (data_quality:<category>:<target>), occurrence counts, and new/changed/reopened/resolved notification states synced on every dashboard build",
      "rationale": "Stable ids make a recurring problem visibly recurring (reopened, count incremented) instead of resetting to a fresh alert each run, which is the difference between a log and an audit trail."
    },
    "threshold_guards": {
      "stale_days": "env HELIOS_DATA_QUALITY_STALE_DAYS clamped 1-3650",
      "min_research_rows": "env HELIOS_DATA_QUALITY_MIN_RESEARCH_ROWS clamped 20-10000",
      "institutional_history_rows": "env HELIOS_DATA_QUALITY_INSTITUTIONAL_ROWS clamped min_research_rows-20000",
      "rationale": "Range guards keep misconfigured environments from silently disabling diagnostics (0 days) or demanding impossible history; the institutional floor can never drop below the research floor."
    }
  }
}
